aapl_stock_classifier/
│
├── config.py                    # DB creds, ticker, feature list, SQL
├── ingest.py                    # Scheduler entry point
├── main.py                      # Training entry point
├── app.py                       # FastAPI REST API
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
├── .env                         # Secrets (never commit)
├── .gitignore
│
├── data/
│   ├── loader.py                # DB connection, yfinance fetch, upsert
│   └── validator.py             # Schema checks, null/outlier detection
│
├── features/
│   ├── engineer.py              # 20+ technical indicators
│   ├── feature_store.py         # Cache engineered features (Parquet/Redis)
│   └── pipeline.py              # Sklearn Pipeline wrapper
│
├── models/
│   ├── train.py                 # RF + XGBoost training
│   ├── evaluate.py              # Metrics, confusion matrix, SHAP
│   ├── tune.py                  # Optuna hyperparameter search
│   └── registry.py              # MLflow model promotion logic
│
├── serving/
│   ├── predict.py               # Prediction engine (load model, run inference)
│   ├── routes.py                # FastAPI route definitions
│   └── schemas.py               # Pydantic request/response models
│
├── monitoring/
│   ├── drift.py                 # Feature/prediction drift detection
│   ├── alerting.py              # Slack/webhook notifications
│   └── metrics.py               # Prometheus metrics export
│
├── pipelines/
│   └── airflow_dag.py           # Airflow DAG (ingest → feature → retrain)
│
├── tests/
│   ├── test_ingestion.py
│   ├── test_features.py
│   ├── test_models.py
│   └── test_api.py
│
├── notebooks/
│   └── exploration.ipynb        # EDA, prototyping
│
└── artifacts/
    ├── models/                  # Saved .pkl files
    └── plots/                   # model_results.png, SHAP plots